# TabPFNCredit – Claude Code Instructions

## Workflow

- **Never use git worktrees.** Always work directly in the main repository directory.
- **Never create new branches.** Work on whatever branch is currently checked out.
- **Never push to GitHub.** The user handles all git pushes via VSCode.
- **Never create PRs automatically.** Only create PRs when explicitly asked.
- Commits are fine to create when asked, but do not push.

## Permissions

- Auto-accept all file edits — do not ask for permission on routine read/write/edit operations.
