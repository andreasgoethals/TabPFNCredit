# ============================================
# TabPFNCredit requirements (shared core)
# ============================================
# Reproducibility note: TALENT is installed from the `talent-tabpfncredit-features`
# branch (which contains the unified registry + Python API + calibration
# metrics + threshold tuning that this repo depends on). Before archiving a
# release, replace the branch with an immutable commit SHA:
#     TALENT @ git+https://github.com/LAMDA-Tabular/TALENT@<40-char-sha>
# ============================================

# ---- Core scientific stack ----
numpy==1.26.4
pandas==2.2.2
scipy==1.13.0
scikit-learn==1.4.2
pyarrow>=14.0.0,<18

# ---- TALENT framework deps ----
delu==0.0.23
einops==0.8.0
qhoptim==1.1.0
category_encoders==2.6.3
dill>=0.3.7,<0.4
msgpack>=1.0.5,<2
msgpack-numpy>=0.4.8,<0.5
torchmetrics>=1.2,<2
pytorch-lightning>=2.1,<2.6     # pinned upper-bound: >=2.6 introduces breaking changes
einx>=0.1.3
safetensors>=0.4.2

# ---- Gradient boosting ----
xgboost==2.1.4
lightgbm==4.6.0
catboost==1.2.8

# ---- Tabular foundation models (used through TALENT) ----
# TALENT delegates to these upstream packages for v3 / v2.5 / TabICL v2 / TabDPT
# instead of bundling checkpoints. Pin the major versions to avoid drift.
tabpfn>=8.0.0,<9                # TabPFN v3 / v2.5 / v2 / v1 (ModelVersion.V*)
tabicl>=2.0.0,<3                # TabICL v2 (classification + regression)
tabdpt>=1.0,<2                  # TabDPT (ICL + retrieval)
# `mitra` is sourced from Amazon's GitHub via TALENT's bundled lib.

# ---- TALENT (use a feature branch with the v3/v2.5/TabICL-v2/TabDPT support;
#      replace with a commit SHA before archiving a release) ----
TALENT @ git+https://github.com/LAMDA-Tabular/TALENT@main

# ---- HPO ----
optuna==3.6.1

# ---- Folds cache + parallelism ----
joblib>=1.3,<2

# ---- Config, CLI & utilities ----
packaging==23.1
PyYAML==6.0.1
tqdm==4.66.4
Requests==2.31.0
typing_extensions>=4.8,<5
huggingface-hub>=0.21,<1
typer>=0.12,<0.13               # public TabPFNCredit CLI (`python -m tabpfncredit`)
rich>=13,<15                    # nice console output for the CLI

# ---- Credit-risk metrics ----
hmeasure==0.1.6
imbalanced-learn==0.11.0
statsmodels>=0.14,<0.15

# ---- Visualisation ----
matplotlib==3.9.2
seaborn==0.13.2
plotly==5.22.0

# ---- Aggregation (replaces pandas-heavy summarize_results) ----
polars>=1.0,<2

# ---- Notebooks ----
notebook>=7.0,<8

# ---- Dev / tests / formatting (install with `pip install -r requirements.txt`) ----
pytest>=8,<9
pytest-xdist>=3,<4              # tests/conftest.py spawns multiprocess for FileLock test
ruff>=0.5,<1
black>=24,<26
isort>=5,<7
pre-commit>=3.7,<4
